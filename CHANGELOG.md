# CHANGELOG

<!--
  This file is auto-generated by `release-it` + `@release-it/conventional-changelog`.
  Do NOT edit it manually — your changes will be overwritten on the next release.
  Release flow guide: docs/guides/release-flow.md
  Quick reference:
    npm run release:dry    — preview the next release (no changes made)
    npm run release:patch  — bump patch version (bug-fixes)
    npm run release:minor  — bump minor version (new features)
    npm run release:major  — bump major version (breaking changes)
  Each release:
    1. Runs pre-release checks (clean branch, pushed commits, lockfile sync, etc.)
    2. Bumps version in package.json
    3. Updates this file from Conventional Commit history
    4. Creates a git tag vX.Y.Z and a GitHub Release
-->
