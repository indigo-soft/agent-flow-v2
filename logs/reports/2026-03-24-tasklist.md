# Documentation Audit — Task List

**Date:** 2026-03-24  
**Source:** [2026-03-24-documentation-audit.md](./2026-03-24-documentation-audit.md)  
**Status legend:** `[ ]` open · `[x]` done · `[-]` skipped/deferred

---

## 🔴 Critical — Must Fix

### ISS-01 — Fix malformed ADR-025 entry in `docs/adr/000_README.md`

- [ ] Move the raw-text line `[025](...) Changelog Automation...` into the Cross-cutting table
      as a proper Markdown table row
- [ ] Verify the table renders correctly in GitHub preview

---

### ISS-06 — Rewrite `docs/adr/023-git-hooks-lefthook.md` implementation section

- [ ] Update the **Implementation → Configuration** example to reflect current `lefthook.yml`
      (native `{staged_files}`, `stage_fixed: true`, no lint-staged)
- [ ] Remove all references to `lint-staged` from the document body and Notes/Примітки section
- [ ] Change "lint-staged залишається, тільки спосіб виклику змінився" → accurate description
- [ ] Fix `--findRelatedTests` → `--passWithNoTests` in test command example
- [ ] Resolve `parallel: true` vs `parallel: false` contradiction — settle on `false` with explanation

---

### ISS-07 — Align `docs/adr/011-code-formatting-prettier.md` with actual `.prettierrc`

- [ ] Update the documented config in ADR-011 to match the actual `.prettierrc`:
  - Change `"trailingComma": "es5"` → `"trailingComma": "all"` and document the rationale
  - Remove `"arrowParens": "always"` and `"bracketSpacing": true` (they are Prettier defaults,
    omitting them is intentional — document this)
- [ ] Add a note explaining that defaults are intentionally omitted for brevity

---

### ISS-08 — Fix CLI commands in `.github/PULL_REQUEST_TEMPLATE.md`

- [ ] Replace `npm test` → `pnpm test`
- [ ] Replace `npm lint` → `pnpm lint`
- [ ] Replace `npm type-check:all` → `pnpm type-check`
- [ ] Add link to `docs/guides/architecture.md` in checklist (ref ISS-23)

---

### ISS-17 — Populate empty GitHub Issue templates

- [ ] Fill `.github/ISSUE_TEMPLATE/bug_report.md` with:
  - YAML front-matter (`name`, `about`, `title`, `labels`, `assignees`)
  - Sections: Description, Steps to Reproduce, Expected Behavior, Actual Behavior,
    Environment, Additional Context
- [ ] Fill `.github/ISSUE_TEMPLATE/feature_request.md` with:
  - YAML front-matter
  - Sections: Problem/Motivation, Proposed Solution, Alternatives Considered,
    Acceptance Criteria, Additional Context

---

### ISS-18 — Handle dead code `.lefthook/commit-format-info.sh`

- [ ] **Decision required:** Integrate into `lefthook.yml` as a `prepare-commit-msg` hook,
      OR delete the script  
- [ ] If integrating: add to `lefthook.yml` under `prepare-commit-msg` hook  
- [ ] If deleting: `rm .lefthook/commit-format-info.sh`  
- [ ] If keeping: at minimum add a comment in `lefthook.yml` explaining it's a manual utility

---

### ISS-25 — Create `.nvmrc` for local Node.js version pinning

- [ ] Create `.nvmrc` at project root with content `24`
- [ ] Mention `.nvmrc` in `README.md` setup section and `docs/guides/deployment.md`
- [ ] Optionally create `.node-version` as well for tools that prefer it (fnm, volta)

---

## 🟡 Important — Should Fix

### ISS-02 — Fix `TEMPLATE-SHORT.md` reference in `docs/adr/000_README.md`

- [ ] **Option A:** Create `docs/adr/templates/TEMPLATE-SHORT.md` — a condensed ADR template
- [ ] **Option B:** Remove the reference from `000_README.md`
- [ ] Update the "Шаблони ADR" section accordingly

---

### ISS-03 — Remove duplicate sentence in `docs/guides/git-workflow.md`

- [ ] Find and remove the duplicate "Без крапки наприкінці" line in the Subject section (~line 145)

---

### ISS-09 — Update pnpm version in `docs/adr/016-package-manager-pnpm.md`

- [ ] Change "pnpm (v8+)" → "pnpm (v10+)" throughout the document
- [ ] Update version benchmarks/examples if they reference old versions
- [ ] Cross-check any other documents that mention "pnpm v8" and update them

---

### ISS-10 — Correct "monorepo" claim in AI instruction files

- [ ] Update `.github/copilot-instructions.md`: change "**Monorepo**: `pnpm` workspaces"
      → "**Package manager**: `pnpm` (single-package project)"
- [ ] Update `.junie/guidelines.md` Tech Stack section: same change
- [ ] If monorepo is a future goal, add a note like "single-package project, monorepo planned"

---

### ISS-11 — Update `docs/adr/012-code-linting-eslint.md` configuration section

- [ ] Replace the documented `.eslintrc.js` configuration with the current `eslint.config.js`
      flat config approach
- [ ] Document: `defineConfig()`, `FlatCompat`, native flat config usage for react/security/next
- [ ] Mention that ESLint v9+ flat config is the project standard
- [ ] Update the list of plugins to match current `eslint.config.js`

---

### ISS-12 — Fix project name in `CONTRIBUTING.md`

- [ ] Replace `ai-workflow-assistant` with `agent-flow-v2` in all git clone URL examples
- [ ] Verify no other stale project names exist in the file

---

### ISS-13 — Document `feature/` → `feat` branch/commit type mapping

- [ ] Add a dedicated note in `docs/guides/git-workflow.md` explaining:
  - Branch prefix `feature/` maps to commit type `feat`
  - Branch prefixes `ci/` and `build/` are intentionally excluded (use `chore/` instead)
- [ ] Add the same clarification in `docs/adr/015-git-workflow-branching-strategy.md`

---

### ISS-19 — Update `.junie/guidelines.md` Section 7 (remove lint-staged)

- [ ] Remove "**lint-staged**: Run linters only on staged files." from Section 7
- [ ] Add accurate description: "**Lefthook** handles staged-file processing natively via
      `{staged_files}` template with `stage_fixed: true` — no lint-staged required"

---

### ISS-20 — ~~Create `.vscode/extensions.json`~~ ✅ N/A — Resolved

> **Resolved (2026-03-24):** Project uses JetBrains IDE, not VS Code. The `.vscode/` folder
> was created by mistake and deleted. `.vscode/` added to `.gitignore` to prevent recurrence.
> Shared editor settings are handled via `.editorconfig` (already present).

- [x] Delete `.vscode/` folder — done
- [x] Add `.vscode/` to `.gitignore` — done

---

### ISS-21 — Translate Ukrainian text in `.github/copilot-instructions.md`

- [ ] Find all Ukrainian phrases in the file (e.g., "⚠️ ВАЖЛИВО: Scope є ОБОВ'ЯЗКОВИМ!")
- [ ] Replace with English equivalents (e.g., "⚠️ IMPORTANT: Scope is REQUIRED!")
- [ ] Keep the document consistently in English

---

### ISS-22 — Define language standard for `.github/` files

- [ ] Decide: English only, or bilingual (English primary, Ukrainian secondary)?
- [ ] If English: translate `BRANCH_NAMING_RULES.md` and `PULL_REQUEST_TEMPLATE.md` to English
- [ ] Document the language policy in `CONTRIBUTING.md`

---

### ISS-26 — Add `engines` field to `package.json`

- [ ] Add to `package.json`:
  ```json
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  }
  ```
- [ ] Verify that `pnpm install` respects the engines field with `engine-strict` setting

---

### ISS-27 — Update `.prettierignore`

- [ ] Add `CHANGELOG.md` — auto-generated, should not be reformatted
- [ ] Add `*.min.js` and `*.min.css` — minified files
- [ ] Consider adding `.idea/` — JetBrains IDE files
- [ ] Verify the current entries are still valid

---

### ISS-28 — Document Qodana usage or remove from CI considerations

- [ ] **Option A (Document):** Add a section to `docs/guides/` explaining how to run Qodana
      locally; add optional Qodana CI job to `ci.yml`; add note to ADR-014 (Tools Summary)
- [ ] **Option B (Remove):** If Qodana is not actively used, remove `qodana.yaml` and document
      the decision

---

## 🔵 Minor — Nice to Have

### ISS-04 — Clarify `parallel` setting in `docs/adr/023-git-hooks-lefthook.md`

- [ ] After ISS-06 rewrite, ensure document consistently uses `parallel: false` in pre-commit
      with a comment explaining why (format must run before lint)

---

### ISS-05 — Remove or create migration guide reference in `ADR-023`

- [ ] Remove the reference to `../MIGRATION_HUSKY_TO_LEFTHOOK.md` from Resources section
      (file does not exist and migration is complete — historical only)

---

### ISS-14 — Clarify SSH vs Token in `docs/guides/release-flow.md`

- [ ] Add a clear "Current approach" section stating the project uses `GITHUB_TOKEN` in `.env`
- [ ] Mention SSH as an alternative with a brief note on how to switch
- [ ] Remove any ambiguity about which approach is recommended

---

### ISS-15 — Fix pnpm workspaces reference in `ADR-016`

- [ ] Add a note: "Note: the project currently uses pnpm as a single-package manager.
      The workspace feature is available for future monorepo expansion."
- [ ] Do not remove workspace documentation — it explains future capability

---

### ISS-16 — Move Ukrainian content in `copilot-instructions.md` to English

- [ ] Covered by ISS-21 (same file, same fix)

---

### ISS-23 — Add architecture links to `.github/PULL_REQUEST_TEMPLATE.md`

- [ ] Add to the checklist:
  ```markdown
  - [ ] Changes comply with [Architecture Guide](../docs/guides/architecture.md)
  - [ ] If architectural decision changed, new ADR created/updated
  ```

---

### ISS-24 — Expand architecture rules in `.junie/guidelines.md`

- [ ] After Section 5 directory tree, add the 4 key enforcement rules:
  1. `modules/` cannot import from other modules (use Event Queue)
  2. `modules/` can import from `components/`
  3. `components/` can import from other `components/`
  4. `components/` cannot import from `modules/` (except `components/api/`)
- [ ] Add example TypeScript import patterns (allowed vs forbidden)

---

### ISS-29 — ~~Add ESLint auto-fix to `.vscode/settings.json`~~ ✅ N/A — Resolved

> **Resolved (2026-03-24):** `.vscode/` folder deleted (project uses JetBrains IDE).
> Configure ESLint auto-fix directly in JetBrains:
> **Settings → Languages & Frameworks → JavaScript → ESLint → Run eslint --fix on save**

- [x] N/A — .vscode/ removed

---

### ISS-30 — ~~Create Dependabot configuration~~ ✅ Done

> **Resolved (2026-03-24):** Added both Renovate (npm) and Dependabot (GitHub Actions).

- [x] `.github/dependabot.yml` — GitHub Actions updates weekly
- [x] `renovate.json` — npm dependency automation (every 3 days, grouped, auto-merge patch/minor)
- [x] `.github/workflows/renovate.yml` — nightly Renovate trigger
- [x] `docs/adr/026-dependency-management.md` — ADR documenting the decision
- [x] `docs/guides/dependency-updates.md` — full guide for developers
- [x] `docs/guides/coding-standards.md` — added Dependency Management section

---

## Summary

| Phase | Total | Done | Remaining |
|---|---|---|---|
| 🔴 Critical | 7 | 0 | 7 |
| 🟡 Important | 14 | 1 | 13 |
| 🔵 Minor | 9 | 2 | 7 |
| **Total** | **30** | **3** | **27** |

---

*Task list generated: 2026-03-24 | Source audit: [2026-03-24-documentation-audit.md](./2026-03-24-documentation-audit.md)*
